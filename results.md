# SecTriage — Accuracy & Performance Results

Generated by `tests/run_eval.py` in **MOCK (deterministic, $0 cost)** mode. Re-run with `python tests/run_eval.py --live` (requires `ANTHROPIC_API_KEY`) to reproduce these numbers against the real LLM analyzer.

## 1. Static code scanner detection accuracy

Measured against `tests/fixtures/vulnerable_app/ground_truth.json`, a hand-authored manifest of 26 labeled lines (true vulnerabilities, true-negative safe code, one deliberately-injected multi-line vulnerability the scanner is expected to miss, and one deliberate false-positive case documenting a known data-flow-analysis limitation).

- **Precision: 94.7%** (18 TP / 19 flagged)
- **Recall: 94.7%** (18 TP / 19 actually vulnerable)
- **F1: 0.95**
- Confusion matrix: TP=18, FP=1, FN=1, TN=6
- Codebase scan wall-clock: 0.015s

### Per-finding breakdown

| File | Line | Pattern | Outcome | Note |
|---|---|---|---|---|
| app.py | 25 | hardcoded_secret | **TP** | Stripe live key literal |
| app.py | 26 | hardcoded_secret | **TP** | AWS access key literal |
| app.py | 27 | hardcoded_secret | **TN** | Loaded from environment, not a literal |
| app.py | 39 | sql_injection | **TP** | f-string interpolation into execute() |
| app.py | 48 | sql_injection | **TP** | String concatenation into execute() |
| app.py | 57 | sql_injection | **TN** | Parameterized query |
| app.py | 66 | sql_injection | **FN** | Query built on one line, executed on the next — line-based scanner cannot see the taint across lines |
| app.py | 74 | xss | **TP** | Unescaped HTML built via string concatenation |
| app.py | 77 | missing_auth_check | **TP** | Unintended-but-valid catch: '/load_config' matches the 'config' keyword hint and has no auth decorator — and genuinely should require auth, since it also deserializes attacker-controlled data via pickle (line 79) |
| app.py | 79 | insecure_deserialization | **TP** | pickle.loads on request body |
| app.py | 85 | insecure_deserialization | **TP** | yaml.load without SafeLoader |
| app.py | 91 | insecure_deserialization | **TN** | yaml.load with explicit SafeLoader |
| app.py | 98 | code_injection | **TP** | eval() on user-controlled expression |
| app.py | 104 | path_traversal | **TP** | Unsanitized filename joined into open() path |
| app.py | 113 | path_traversal | **TN** | Filename sanitized with secure_filename() first |
| app.py | 121 | ssrf | **TP** | Outbound request to user-controlled URL, no allowlist |
| app.py | 128 | command_injection | **TP** | os.system() with concatenated user input |
| app.py | 132 | missing_auth_check | **TP** | Sensitive route with no auth decorator |
| app.py | 151 | missing_auth_check | **TN** | Route path has no sensitive keyword, heuristic should not fire |
| static/app.js | 4 | hardcoded_secret | **TP** | Hardcoded API key literal |
| static/app.js | 8 | xss | **TP** | innerHTML assigned from a variable |
| static/app.js | 13 | xss | **TN** | textContent does not parse HTML |
| static/app.js | 17 | xss | **TP** | document.write() with a variable |
| static/app.js | 21 | insecure_deserialization | **TP** | eval() on user-controlled code |
| static/app.js | 25 | ssrf | **TP** | fetch() on a user-controlled URL, no allowlist |
| static/app.js | 33 | ssrf | **FP** | URL is allowlist-checked before fetch(); the line-based scanner has no data-flow analysis and cannot see the check above, so it is expected to (incorrectly) flag this line |

## 2. End-to-end pipeline throughput vs. assumed manual baseline

Full pipeline (Nmap + ZAP + code review → LLM triage → dedup) run against all three sample fixtures: **30 findings** parsed, **29 unique** after cross-tool dedup (1 merged).

- Pipeline wall-clock: **3.02s** (0.101s/finding avg, mock analyzer)
- Assumed manual review baseline: **15 min/finding** (industry rule-of-thumb for locating, classifying, and writing up one finding by hand — *not independently measured in this project*; treat as a directional assumption, not a controlled study) → 450 minutes total for 30 findings.
- Time-reduction percentage: **not computed in mock mode** — see cost note below. A mock-mode wall-clock is local-code overhead, not model latency, so dividing it into the manual baseline would produce a meaningless near-100% number rather than a real measurement.

> **Cost note:** this run used the free mock analyzer (no API calls, no tokens). It measures pipeline plumbing overhead (parsing, dedup, report rendering), NOT real LLM latency or cost. Run `python tests/run_eval.py --live` with a Claude API key to get real per-finding latency, token usage, prompt-cache hit rate, and a genuine time-reduction figure for the `mock-analyzer` analyzer.

## Methodology notes and honest caveats

- The fixture app (`tests/fixtures/vulnerable_app`) was written *for this project*, with the ground-truth manifest derived from inline marker comments in the source — it is not an independent, blind test set, and the scanner's regex rules were developed alongside it. Treat the accuracy numbers above as a controlled unit-test result, not a claim about real-world codebases.
- The manual-review-time baseline (15 min/finding) is a stated assumption, not something measured against a human reviewer in this project. It is a plausible order-of-magnitude figure for security triage work, not a citation-backed number.
- Mock-mode timing measures pipeline overhead only; it is not representative of real LLM API latency. Live-mode numbers (see above) are the ones that reflect actual model latency, token cost, and prompt-cache behavior.
- The static scanner is line-based pattern matching, not full data-flow/taint analysis — see the FN and FP rows in the breakdown table above for concrete, deliberately-included examples of both failure modes.
